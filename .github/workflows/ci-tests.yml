name: CI Tests
on: 
  push: 
    branches-ignore:
      - develop
  pull_request:
  workflow_call:
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.0.2
      - run: |
          cd react-web
          npm ci
          npm run test